# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

node_modules
dist
dist-ssr
*.local

# Local env / secrets — never commit real API keys
.env
.env.*
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Frontend build output
frontend/dist

# Backend runtime data (SQLite + 落盘图片)
backend/data

# Dev run logs (stray)
dev.err
dev.out

# e2e 运行时生成的 seed（含 storyId，按需重建，勿提交）
e2e/seed.json
pw-install.log
pw-install.err
